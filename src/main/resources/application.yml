spring:
  application:
    name: jpa

  datasource:
    url: jdbc:postgresql://localhost:5432/bookstore-jpa
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creative: true